[package]
name = "corridor_eligibility"
type = "bin"
authors = ["Sconce Labs"]

[dependencies]
# Poseidon2 over BN254. This permutation must match Soroban's
# `poseidon2_permutation` host function and the Compact tree hashing — see the
# conformance test tracked in the roadmap (correctness gate).
poseidon = { tag = "v0.3.0", git = "https://github.com/noir-lang/poseidon" }
